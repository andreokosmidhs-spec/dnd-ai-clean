import React from 'react';

// Removed — stats are shown in CharacterSidebar. Null stub kept so any
// residual references don't crash during deployment.
const GameDetailsDrawer = () => null;

export default GameDetailsDrawer;
